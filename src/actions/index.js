import * as AuthActionTypes from 'actions/auth';
import * as EntityActionTypes from 'actions/entities';
import * as CartActionTypes from 'actions/cart';

export default { AuthActionTypes, EntityActionTypes, CartActionTypes };
